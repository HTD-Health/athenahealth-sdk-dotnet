using System.Collections;
using System.Collections.Generic;

// ReSharper disable IdentifierTypo
// ReSharper disable CommentTypo
// ReSharper disable StringLiteralTypo
namespace AthenaHealth.Sdk.Tests.EndToEnd.Data.Patient
{
    public class GetPatientsData : IEnumerable<object[]>
    {
        public IEnumerator<object[]> GetEnumerator()
        {
            int iterations = 0;

            for (int i = 0; i <= iterations; i++)
            {
                yield return new object[] { "John", i == 0 ? (int?)null : i * 10 };
            }

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Sarah", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Adam", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Matt", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Martha", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Gregory", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "John", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Peter", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Patrick", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Albert", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "George", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "David", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Noah", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Emma", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Olivia", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Isabella", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Sophia", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Charlotte", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Amelia", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Evelyn", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Emily", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Walter", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "William", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "James", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Lucas", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Alexander", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Logan", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Mason", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Ethan", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Jacob", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Michael", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Daniel", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Henry", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Harry", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Sebastian", i == 0 ? (int?)null : i * 10 };
            //}

            //for (int i = 0; i <= iterations; i++)
            //{
            //    yield return new object[] { "Matthew", i == 0 ? (int?)null : i * 10 };
            //}
        }

        IEnumerator IEnumerable.GetEnumerator() => GetEnumerator();
    }
}
